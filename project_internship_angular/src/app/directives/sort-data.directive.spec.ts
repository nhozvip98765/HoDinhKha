import { SortDataDirective } from './sort-data.directive';

describe('SortDataDirective', () => {
  it('should create an instance', () => {
    const directive = new SortDataDirective();
    expect(directive).toBeTruthy();
  });
});
